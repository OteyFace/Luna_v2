module.exports = {

    name        : "name",
    description : "description",
    cooldown    : 1,
    aliases     : ["alias"],
    guildOnly   : true,
    privileges  : ["SEND_MESSAGES"],
    usage       : "cmd",
    
    async execute(interaction) {
        // runs on execution
    }
};